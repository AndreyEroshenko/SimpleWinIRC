<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SimpleWinIRC.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleWinIRC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SimpleWinIRC">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="ServerComboBox" Header="Server" IsEditable="True" Text="irc.irchighway.net" HorizontalAlignment="Stretch" SelectionChanged="ServerComboBox_SelectionChanged">
                <x:String>irc.irchighway.net</x:String>
                <x:String>irc.undernet.org</x:String>
            </ComboBox>
            <NumberBox Grid.Column="1" x:Name="PortNumberBox" Header="Port" Value="9999" Minimum="1" Maximum="65535" SpinButtonPlacementMode="Compact" />
            <TextBox Grid.Column="2" x:Name="NicknameTextBox" Header="Nickname" PlaceholderText="your_nick" />
            <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Bottom" Margin="0,0,0,4">
                <CheckBox x:Name="UseSslCheckBox" Content="SSL" IsChecked="True" />
                <CheckBox x:Name="IgnoreCertCheckBox" Content="Ignore cert errors" />
            </StackPanel>
            <Button Grid.Column="4" x:Name="ConnectButton" Content="Connect" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Bottom" Click="ConnectButton_Click" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ChannelTextBox" Header="Channel" PlaceholderText="#channel" IsEnabled="False" KeyDown="ChannelTextBox_KeyDown" />
            <Button Grid.Column="1" x:Name="JoinButton" Content="Join" IsEnabled="False" VerticalAlignment="Bottom" Click="JoinButton_Click" />
        </Grid>

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="4" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <ScrollViewer x:Name="OutputScrollViewer" VerticalScrollBarVisibility="Auto" Padding="8">
                <TextBlock x:Name="OutputTextBlock" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="InputTextBox" PlaceholderText="Type an IRC command, e.g. JOIN #channel, PRIVMSG #channel :hi" IsEnabled="False" KeyDown="InputTextBox_KeyDown" />
            <Button Grid.Column="1" x:Name="SendButton" Content="Send" IsEnabled="False" Click="SendButton_Click" />
        </Grid>

        <TextBlock Grid.Row="4" x:Name="StatusTextBlock" Text="Not connected." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</Window>
